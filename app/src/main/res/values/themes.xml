<resources>
    <style name="Theme.AppBlocker" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/ss_background</item>
        <item name="colorPrimary">@color/ss_primary</item>
        <item name="colorSurface">@color/ss_card</item>
    </style>

    <!-- Dark AlertDialog theme for the "Add" dialogs -->
    <style name="BlockerDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="android:background">@color/ss_card</item>
        <item name="android:textColor">@color/ss_foreground</item>
        <item name="colorPrimary">@color/ss_primary</item>
    </style>
</resources>
